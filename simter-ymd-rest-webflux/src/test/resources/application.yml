spring:
  main:
    banner-mode: 'off'
logging:
  #file: target/test.log
  level:                               # one of TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF
    root: ERROR
    org.springframework: WARN